/* Determines whether an alert should represent a successful (green)
  or unsuccessful action (red) */
export enum AlertType {
  SUCCESS = 'SUCCESS',
  ERROR = 'ERROR'
}

export enum SubmitEvent {
  START = 'START',
  END = 'END'
}
